import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
`;

const Content = styled.div`
  padding: 26px 20px;
  width: 100%;
`;

export const Styled = {
  Wrapper,
  Content,
};
