import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  margin-right: 20px;
`;

const Container = styled.div`
  display: flex;
`;

export const Styled = {
  Wrapper,
  Container,
};
